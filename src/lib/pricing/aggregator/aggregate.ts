import type { PricingScenario, RateRow, UnifiedQuote } from "../domain";

/**
 * Given scenario inputs and normalized rate rows, return the unified comparison table.
 */
export function aggregate(
  _scenario: PricingScenario,
  _rows: RateRow[],
): UnifiedQuote[] {
  return [];
}
